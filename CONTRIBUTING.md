# 기여 방법

## 시작

README의 "실행" 두 블록을 따라가면 로컬에서 뜬다. 백엔드 8000, 프론트 5173.

## 브랜치 & PR

- 브랜치: `feat/…`, `fix/…`, `docs/…`
- PR 하나 = 변경 하나. 리뷰할 수 있는 크기로.
- PR 본문에: 뭘 바꿨는지, 왜, 어떻게 확인했는지 (스크린샷이면 충분).

## 손대기 전에 알아둘 것

- **API 계약을 깨지 마라.** 노드는 항상 좌표 3종을 갖는다. 뷰를 추가하려면
  `store.VIEWS`, `seed.py`, `index.html`의 버튼을 같이 고친다.
- **좌표는 백엔드가 계산한다.** 프론트는 받은 좌표를 그릴 뿐, 레이아웃을 만들지 않는다.
  (그래서 `three-forcegraph`를 쓰지 않는다.)
- **로컬 모델만.** 유료 추론 API는 받지 않는다.

## 확인

```bash
cd backend && .venv/bin/python store.py   # 스키마 검증 통과해야 함
cd frontend && npm run build              # 빌드 깨지지 않아야 함
```

테스트 프레임워크는 아직 없다. 로직을 추가하면 `store.py`의 `demo()`처럼
`assert` 몇 줄짜리 자체 점검을 같은 파일에 붙여라.
