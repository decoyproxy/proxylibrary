fastapi
uvicorn[standard]
# Phase 2 — local embeddings, vector store, 3D projection. No paid APIs.
sentence-transformers
chromadb
umap-learn
# Phase 3 — multi-format ingest
pymupdf         # PDF text
open_clip_torch # image vectors (separate space from the text model)
watchdog        # re-ingest on file changes
# Phase 13 — OCR through macOS Vision (no model download, reads Korean)
pyobjc-framework-Vision; sys_platform == "darwin"
